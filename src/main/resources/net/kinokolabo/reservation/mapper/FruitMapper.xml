<?xml version="1.0" encoding="UTF-8" ?>
        <!DOCTYPE mapper
                PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="net.kinokolabo.reservation.mapper.FruitMapper">
    <!--<insert id="insert" useGeneratedKeys="true" keyProperty="id">-->
        <!--INSERT INTO todo (title, details, finished) VALUES (#{title}, #{details}, #{finished})-->
    <!--</insert>-->
    <select id="selectAll" resultType="net.kinokolabo.reservation.domain.Fruit">
        SELECT name FROM fruits
    </select>
</mapper>